#include<stdio.h>
int main(){

    printf("enter string\n");
    int b[10]={2,3,4,5,6,7};
// fgets(b,sizeof(b),stdin);
// gets(b);   
//  for(int i=0;i<sizeof(b);i++){
    // printf("valuev %c is=%d\n ",b[i],(int)b[i]);
    // }

    // puts(b);
    printf("%S",b);
        return 0;
}